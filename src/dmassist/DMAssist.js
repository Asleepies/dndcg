import React, {useState} from 'react';
import { NewPlayer } from './components/NewPlayer';
import { PlayerContainer } from './containers/PlayerContainer.js';

export const DMAssist = () => {

  return (
    <div>
      <PlayerContainer />
    </div>
  )
}